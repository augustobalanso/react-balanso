import { createTheme } from '@mui/material/styles';

const theme = createTheme({

palette: {
    type: 'light',
    primary: {
      main: '#125B50',
    },
    secondary: {
      main: '#D96C06',
    },
  },
})

export default theme